<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Politics.CoronaStatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Politics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ct="using:CoronaCharts" xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Loaded="Page_Loaded">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <TabView>
            <TabViewItem Header="Activity">
                <TabViewItem.Content>
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RowDefinitions="*" ColumnDefinitions="1.5*,Auto,*,Auto,3*">
                        <ScrollViewer Grid.Column="0" Grid.Row="0" x:Name="TargetScrollViewer"
                                HorizontalScrollBarVisibility="Auto"
                                VerticalScrollBarVisibility="Visible">
                                <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                                    <TextBlock Text="Legend" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"></TextBlock>
                                    <ct:LegendControl x:Name="legendControl"></ct:LegendControl>
                                </StackPanel>
                            </ScrollViewer>
                        <controls:GridSplitter Background="Gray"  Grid.Column="1" Grid.Row="0" Width="2" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"></controls:GridSplitter>
                        <StackPanel Grid.Column="2" Grid.Row="0" Spacing="8">
                            <TextBlock Text="Avg Milliseconds" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"></TextBlock>
                            <ct:BarChart x:Name="performanceChart" Width="250" Height="250"></ct:BarChart>
                            <TextBlock Text="Execution Share" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"></TextBlock>
                            <ct:PieChart x:Name="distributionChart" Width="250" Height="250"></ct:PieChart>
                        </StackPanel>
                        <controls:GridSplitter Background="Gray" Grid.Column="3"  Grid.Row="0" Width="2" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"></controls:GridSplitter>
                        <local:CoronaActivity x:Name="activitygrid" Grid.Column="4" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></local:CoronaActivity>
                    </Grid>
                </TabViewItem.Content>
            </TabViewItem>
        </TabView>
    </Grid>
</Page>
