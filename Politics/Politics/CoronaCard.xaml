<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Politics.CoronaCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Politics"
    xmlns:models="using:Politics.models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <Border Background="White" BorderBrush="Gray" BorderThickness="2" Margin="0,2,0,2" Padding="4" CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding title}" HorizontalAlignment="Center" VerticalAlignment="Center" TextDecorations="Underline"/>
            <ListView Grid.Row="1"  ItemsSource="{Binding items}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:cobjectitem">
                        <StackPanel Orientation="Horizontal" >
                            <TextBlock Text="{Binding field_name, Mode=OneWay}" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding field_value, Mode=OneWay}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</UserControl>
