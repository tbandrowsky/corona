<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Politics.Donor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Politics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cty="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0" Grid.Row="0">
            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">Full Name</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding full_name, Mode=TwoWay}" IsReadOnly="True"/>

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">City</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding city, Mode=TwoWay}" IsReadOnly="True"/>

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">State</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding state, Mode=TwoWay}" IsReadOnly="True"/>

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">Zip</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding zip, Mode=TwoWay}" IsReadOnly="True"/>

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">Employer</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding employer, Mode=TwoWay}" IsReadOnly="True"/>

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">Occupation</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding occupation, Mode=TwoWay}" IsReadOnly="True"/>
        </StackPanel>
        
        <cty:DataGrid Grid.Column="1" Grid.Row="0" x:Name="SchemaGrid" AutoGenerateColumns="false">
            <cty:DataGrid.Columns>
                <cty:DataGridTextColumn Header="Transaction Date" Binding="{Binding transaction_date}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Transaction Amount" Binding="{Binding transaction_amount}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Full Name" Binding="{Binding full_name}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Candidate Id" Binding="{Binding candidate_id}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Political Party" Binding="{Binding political_party}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Election Year" Binding="{Binding election_year}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Street 1" Binding="{Binding street1}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Street 2" Binding="{Binding street2}" Width="Auto"/>
                <cty:DataGridTextColumn Header="City" Binding="{Binding city}" Width="Auto"/>
                <cty:DataGridTextColumn Header="State" Binding="{Binding state}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Zip" Binding="{Binding zip}" Width="Auto"/>
            </cty:DataGrid.Columns>
        </cty:DataGrid>
    </Grid>
</UserControl>
