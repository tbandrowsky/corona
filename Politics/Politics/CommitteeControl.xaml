<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Politics.CommitteeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Politics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Style="{StaticResource TitleTextBlockStyle}" Text="Committee Details" />
        <StackPanel Grid.Column="0" Grid.Row="1">
            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">Full Name</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding committee_name}" IsReadOnly="True"/>

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">Candidate Id</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding committee_id}" IsReadOnly="True"/>

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}">Political Party</TextBlock>
            <TextBox Style="{StaticResource BodyTextBoxStyle}" Text="{Binding political_party}" IsReadOnly="True"/>
        </StackPanel>

    </Grid>
</UserControl>
