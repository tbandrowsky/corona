<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Politics.ErrorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Politics"
    xmlns:ci="using:CoronaInterface"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Visibility="{x:Bind StatusDisplay, Mode=OneWay}">

    <Grid Padding="4">
        <Grid.Resources>
            <Style x:Key="TextBlockColor" TargetType="TextBlock">
                <Setter Property="Foreground" Value="White"/>
            </Style>
        </Grid.Resources>
        <Border CornerRadius="4" Margin="4" BorderThickness="2">
            <Border.Background>
                <SolidColorBrush Color="{x:Bind StatusColorBackground, Mode=OneWay}" />
            </Border.Background>
            <Border.BorderBrush>
                <SolidColorBrush Color="{x:Bind StatusBorder, Mode=OneWay}" />
            </Border.BorderBrush>
            <StackPanel Padding="8">
                <TextBlock Style="{StaticResource TextBlockColor}" Text="{x:Bind Message, Mode=OneWay}" TextWrapping="Wrap" />
                <TextBlock Style="{StaticResource TextBlockColor}" Text="{x:Bind ElapsedMS, Mode=OneWay}" />

                <ListView ItemsSource="{x:Bind Errors, Mode=OneWay}" Margin="0,8,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="ci:CoronaError">
                            <StackPanel>
                                <TextBlock Style="{StaticResource TextBlockColor}" Text="{x:Bind class_name, Mode=OneWay}" TextWrapping="Wrap"/>
                                <TextBlock Style="{StaticResource TextBlockColor}" Text="{x:Bind field_name, Mode=OneWay}" TextWrapping="Wrap"/>
                                <TextBlock Style="{StaticResource TextBlockColor}" Text="{x:Bind message, Mode=OneWay}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                
                <Button Content="Dismiss" Click="Button_Click" HorizontalAlignment="Right" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
