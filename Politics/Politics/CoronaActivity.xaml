<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Politics.CoronaActivity"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Politics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cty="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <cty:DataGrid x:Name="SchemaGrid" AutoGenerateColumns="false">
            <cty:DataGrid.Columns>
                <cty:DataGridTextColumn Header="Api" Binding="{Binding Api}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Topic" Binding="{Binding Topic}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="Auto"/>
                <cty:DataGridTextColumn Header="StartTime" Binding="{Binding StartTimeString}" Width="Auto"/>
                <cty:DataGridTextColumn Header="Elapsed MS" Binding="{Binding ElapsedMillisecondsString}" Width="Auto"/>
            </cty:DataGrid.Columns>
        </cty:DataGrid>
    </Grid>
</UserControl>
