<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Politics.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Politics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cty="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    xmlns:model="using:Politics.models" 
    xmlns:cty1="using:CommunityToolkit.WinUI.Controls">
            
    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="4" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Search" Margin="16,0,0,0"></TextBlock>
            <TextBox x:Name="SearchBox" PlaceholderText="Search..." Margin="12" Text="{Binding SearchText,Mode=TwoWay}"/>
            <Button x:Name="SearchButton" Content="Search"  Margin="12" Click="SearchButton_Click" />
            <local:ErrorControl x:Name="ErrorControl"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" Grid.Column="0" 
                      Margin="12">
            <ListView x:Name="ResultsListView" ItemsSource="{Binding SearchResults, Mode=OneWay}" 
                      SelectionMode="Single" 
                      SelectionChanged="ListView_SelectionChanged"
                      ItemClick="ListView_ItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:Card CObject="{Binding Mode=OneWay}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        <cty1:GridSplitter Background="LightGray"  Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Width="1" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"/>
        <local:DonorControl x:Name="DonorControl" Grid.Row="0" Grid.RowSpan="2" Grid.Column="2" Visibility="Collapsed" Margin="8,0,0,0" IsEnabled="false"/>
        <local:CandidateControl x:Name="CandidateControl" Grid.Row="0" Grid.RowSpan="2" Grid.Column="2" Visibility="Collapsed" Margin="8,0,0,0" IsEnabled="false"/>
    </Grid>
</Page>
