<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CoronaCharts.BarChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoronaCharts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:win2dcanvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <local:YAxisControl x:Name="YAxis" Grid.Row="0" Grid.Column="0"></local:YAxisControl>
        <local:XAxisControl x:Name="XAxis" Grid.Row="1" Grid.Column="1"></local:XAxisControl>
        <win2dcanvas:CanvasControl
            x:Name="Canvas"
            Grid.Row="0"
            Grid.Column="1"
            ClearColor="White"
            Draw="Canvas_Draw"
            CreateResources="Canvas_CreateResources"/>    
    </Grid>
</UserControl>
