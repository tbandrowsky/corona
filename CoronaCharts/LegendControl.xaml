<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CoronaCharts.LegendControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoronaCharts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Resources>
            <CollectionViewSource
                x:Name="GroupedLegend"
                x:Key="GroupedLegend"/>
        </Grid.Resources>
        <ListView ItemsSource="{x:Bind GroupedLegend.View, Mode=OneWay}" SelectionMode="None">
            <ListView.GroupStyle>
                <GroupStyle >
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate x:DataType="local:LegendByApi">
                            <Border>
                                <TextBlock Text="{Binding Api}" Style="{ThemeResource BodyTextBlockStyle}"/>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel AreStickyGroupHeadersEnabled="False"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" VerticalAlignment="Center">
                        <CheckBox.Content>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <Rectangle Width="75" Height="20" Stroke="Black" StrokeThickness="1" Margin="0,0,10,0">
                                    <Rectangle.Fill>
                                        <SolidColorBrush Color="{Binding Color}"/>
                                    </Rectangle.Fill>
                                </Rectangle>
                                <TextBlock Text="{Binding Label}" VerticalAlignment="Center" />
                                <Button Click="Button_Click" DataContext="{Binding}">
                                    <Button.Content>
                                        <SymbolIcon Symbol="Go"/>
                                    </Button.Content>
                                </Button>
                            </StackPanel>
                        </CheckBox.Content>
                    </CheckBox>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
